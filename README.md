# SDC JavaScript/TypeScript Tasks for Practice

## JavaScript

### Numbers

Sum of Numbers: Write a function that takes an array of numbers and returns their sum. For example, for the array [1, 2, 3, 4], the result should be 10.

Find Maximum Number: Create a function that takes an array of numbers and returns the maximum number from that array. For example, for the array [5, 2, 9, 1], the result should be 9.

Check Evenness: Write a function that takes a number and returns true if the number is even and false if it is odd. For example, for the number 4, the result should be true, and for 5, it should be false.

Factorial of a Number: Create a function that calculates the factorial of a given number. For example, for the number 5, the result should be 120 (5! = 5 × 4 × 3 × 2 × 1).

Check Prime Number: Write a function that takes a number and returns true if the number is prime and false otherwise. For example, for the number 7, the result should be true, and for 8, it should be false.

Sort Array: Create a function that takes an array of numbers and returns a new array sorted in ascending order. For example, for the array [3, 1, 4, 2], the result should be [1, 2, 3, 4].

Power Function: Write a function that takes two numbers: a base and an exponent, and returns the result of raising the base to the exponent. For example, for the base 2 and exponent 3, the result should be 8.

Calculate Average: Create a function that takes an array of numbers and returns their average. For example, for the array [2, 4, 6, 8], the result should be 5.

Generate Random Number: Write a function that returns a random integer within a specified range (inclusive). For example, for the range from 1 to 10, the function should return a random number from 1 to 10.

Convert Celsius to Fahrenheit: Create a function that takes a temperature in Celsius and returns its equivalent in Fahrenheit. The conversion formula is F = C \* 9/5 + 32. For example, for 0 degrees Celsius, the result should be 32 degrees Fahrenheit.

Convert Fahrenheit to Celsius: Create a function that takes a temperature in Fahrenheit and returns its equivalent in Celsius. The conversion formula is C = (F - 32) \* 5/9. For example, for 32 degrees Fahrenheit, the result should be 0 degrees Celsius.

Calculate Percentage: Write a function that takes two numbers: a total amount and a percentage, and returns the result of calculating the percentage of the total amount. For example, for a total of 100 and a percentage of 25, the result should be 25.

Calculate Square Root: Create a function that takes a number and returns its square root. For example, for the number 16, the result should be 4.

Calculate Absolute Value: Write a function that takes a number and returns its absolute value. For example, for the number -5, the result should be 5.

Round to Nearest Integer: Create a function that takes a number and returns its rounded value to the nearest integer. For example, for the number 4.7, the result should be 5.

Round to Specified Precision: Write a function that takes a number and the number of decimal places, and returns its rounded value to the specified precision. For example, for the number 4.756 and precision 2, the result should be 4.76.

Calculate Difference Between Two Numbers: Create a function that takes two numbers and returns their difference. For example, for the numbers 10 and 5, the result should be 5.

Calculate Sum of Squares: Write a function that takes an array of numbers and returns the sum of their squares. For example, for the array [1, 2, 3], the result should be 14 (1² + 2² + 3²).

Calculate Product of Numbers: Create a function that takes an array of numbers and returns their product. For example, for the array [1, 2, 3], the result should be 6 (1 × 2 × 3).

Calculate Distance Between Two Points: Write a function that takes the coordinates of two points (x1, y1) and (x2, y2) and returns the distance between them. The formula for calculating the distance is √((x2 - x1)² + (y2 - y1)²). For example, for the points (1, 2) and (4, 6), the result should be 5.

Check Palindrome Number: Write a function that takes a number and returns true if it is a palindrome (the same backward as forward) and false otherwise. For example, for the number 121, the result should be true, and for 123, it should be false.

Find GCD (Greatest Common Divisor): Create a function that takes two numbers and returns their greatest common divisor. For example, for the numbers 8 and 12, the result should be 4.

Find LCM (Least Common Multiple): Write a function that takes two numbers and returns their least common multiple. For example, for the numbers 4 and 5, the result should be 20.

Check Armstrong Number: Create a function that determines if a number is an Armstrong number (a number that is equal to the sum of its own digits each raised to the power of the number of digits). For example, for the number 153, the result should be true (1³ + 5³ + 3³ = 153).

Count Digits in a Number: Write a function that takes a number and returns the count of its digits. For example, for the number 12345, the result should be 5.

Reverse a Number: Create a function that takes a number and returns it reversed. For example, for the number 12345, the result should be 54321.

Generate Fibonacci Sequence: Write a function that takes a number n and returns the first n numbers in the Fibonacci sequence. For example, for n = 5, the result should be [0, 1, 1, 2, 3].

Calculate Compound Interest: Create a function that calculates the compound interest given the principal amount, rate of interest, number of times interest applied per time period, and the number of time periods. For example, for a principal of 1000, rate 5%, compounded 4 times a year for 5 years, the result should be calculated accordingly.

Find the Median of an Array: Write a function that takes an array of numbers and returns the median value. For example, for the array [3, 1, 2, 5, 4], the result should be 3.

Check for Perfect Number: Create a function that checks if a number is a perfect number (a number that is equal to the sum of its proper divisors). For example, for the number 6, the result should be true (1 + 2 + 3 = 6).

Count Vowels in a Number: Write a function that takes a number (converted to a string) and returns the count of vowels in its string representation. For example, for the number 12345, the result should be 0.

Find the Mode of an Array: Create a function that takes an array of numbers and returns the mode (the number that appears most frequently). For example, for the array [1, 2, 2, 3, 4], the result should be 2.

Calculate the Sum of Digits: Write a function that takes a number and returns the sum of its digits. For example, for the number 123, the result should be 6 (1 + 2 + 3).

Check if a Number is a Perfect Square: Create a function that checks if a given number is a perfect square. For example, for the number 16, the result should be true, and for 15, it should be false.

Convert Decimal to Binary: Write a function that converts a decimal number to its binary representation. For example, for the number 10, the result should be 1010.

Convert Binary to Decimal: Create a function that converts a binary number (as a string) to its decimal equivalent. For example, for the binary string '1010', the result should be 10.

Find the Range of an Array: Write a function that takes an array of numbers and returns the range (the difference between the maximum and minimum values). For example, for the array [3, 1, 4, 1, 5], the result should be 4 (5 - 1).

Calculate the Harmonic Mean: Create a function that calculates the harmonic mean of an array of numbers. For example, for the array [1, 2, 3], the harmonic mean should be 1.636.

Check if Two Numbers are Coprime: Write a function that checks if two numbers are coprime (their greatest common divisor is 1). For example, for the numbers 8 and 15, the result should be true.

Generate a Random Floating-Point Number: Create a function that generates a random floating-point number within a specified range (inclusive). For example, for the range from 1.0 to 10.0, the function should return a random floating-point number between 1.0 and 10.0.

### Strings

Reverse a String: Write a function that takes a string and returns it reversed. For example, for the input "hello", the output should be "olleh".

Check for Palindrome: Create a function to check if a string is a palindrome (reads the same backward as forward). For example, "racecar" should return true, while "hello" should return false.

Count Vowels in a String: Write a function that counts the number of vowels in a given string. For example, for the string "hello world", the result should be 3.

Count Consonants in a String: Create a function that counts the number of consonants in a given string. For example, for the string "hello world", the result should be 7.

Find the Length of a String: Write a function that returns the length of a given string. For example, for the string "hello", the result should be 5.

Convert to Uppercase: Create a function that converts a given string to uppercase. For example, for the string "hello", the result should be "HELLO".

Convert to Lowercase: Write a function that converts a given string to lowercase. For example, for the string "HELLO", the result should be "hello".

Capitalize the First Letter: Create a function that capitalizes the first letter of each word in a string. For example, for the string "hello world", the result should be "Hello World".

Remove Whitespace: Write a function that removes all whitespace from a string. For example, for the string " hello world ", the result should be "helloworld".

Find a Substring: Create a function that checks if a given substring exists within a string. For example, for the string "hello world" and substring "world", the result should be true.

Replace a Substring: Write a function that replaces all occurrences of a substring within a string. For example, for the string "hello world" and replacing "world" with "there", the result should be "hello there".

Split a String into an Array: Create a function that splits a string into an array of words based on spaces. For example, for the string "hello world", the result should be ["hello", "world"].

Join an Array into a String: Write a function that joins an array of strings into a single string with a specified separator. For example, for the array ["hello", "world"] with a separator " ", the result should be "hello world".

Check if String Contains Only Digits: Create a function that checks if a string contains only numeric digits. For example, for the string "12345", the result should be true, and for "123a5", it should be false.

Find the First Non-Repeated Character: Write a function that finds the first non-repeated character in a string. For example, for the string "swiss", the result should be 'w'.

Count the Number of Words: Create a function that counts the number of words in a string. For example, for the string "hello world", the result should be 2.

Check for Anagrams: Write a function that checks if two strings are anagrams of each other. For example, "listen" and "silent" should return true.

Extract Digits from a String: Create a function that extracts all digits from a given string and returns them as a new string. For example, for the string "abc123def", the result should be "123".

Remove Duplicates from a String: Write a function that removes duplicate characters from a string while preserving the order. For example, for the string "hello", the result should be "helo".

Convert to Title Case: Create a function that converts a string to title case (first letter of each word capitalized). For example, for the string "hello world", the result should be "Hello World".

Find the Index of a Character: Write a function that returns the index of the first occurrence of a character in a string. For example, for the string "hello" and character 'l', the result should be 2.

Count Specific Character Occurrences: Create a function that counts the occurrences of a specific character in a string. For example, for the string "hello" and character 'l', the result should be 2.

Trim Leading and Trailing Spaces: Write a function that trims leading and trailing whitespace from a string. For example, for the string " hello ", the result should be "hello".

Check if String Starts with a Specific Substring: Create a function that checks if a string starts with a specific substring. For example, for the string "hello world" and substring "hello", the result should be true.

Check if String Ends with a Specific Substring: Write a function that checks if a string ends with a specific substring. For example, for the string "hello world" and substring "world", the result should be true.

Find the Last Occurrence of a Character: Create a function that finds the last occurrence of a character in a string. For example, for the string "hello" and character 'l', the result should be 3.

Convert String to Array of Characters: Write a function that converts a string into an array of its characters. For example, for the string "hello", the result should be ['h', 'e', 'l', 'l', 'o'].

Shuffle the Characters of a String: Create a function that shuffles the characters of a string randomly. For example, for the string "hello", the result could be "lhol" or any other random permutation.

Count the Number of Unique Characters: Write a function that counts the number of unique characters in a string. For example, for the string "hello", the result should be 4.

Find the Longest Word in a String: Create a function that finds the longest word in a given string. For example, for the string "The quick brown fox", the result should be "quick".

Check for Substring at Specific Index: Write a function that checks if a substring exists at a specific index in a string. For example, for the string "hello" and substring "ll" at index 2, the result should be true.

Repeat a String N Times: Create a function that repeats a string n times. For example, for the string "hello" and n = 3, the result should be "hellohellohello".

Convert a String to a Number: Write a function that converts a string representation of a number to an actual number. For example, for the string "123", the result should be 123.

Find the Average Length of Words in a String: Create a function that calculates the average length of words in a string. For example, for the string "hello world", the result should be 5.

Check if Two Strings are Equal (Case Insensitive): Write a function that checks if two strings are equal, ignoring case. For example, "hello" and "HELLO" should return true.

Remove All Digits from a String: Create a function that removes all digits from a string. For example, for the string "abc123def", the result should be "abcdef".

Find the First Word in a String: Write a function that returns the first word in a string. For example, for the string "hello world", the result should be "hello".

Check if a String is Empty: Create a function that checks if a string is empty. For example, for the string "", the result should be true.

Convert a String to a URL-friendly Format: Write a function that converts a string into a URL-friendly format (e.g., spaces replaced with hyphens, lowercase). For example, for the string "Hello World", the result should be "hello-world".

Find All Unique Words in a String: Create a function that finds all unique words in a string and returns them in an array. For example, for the string "hello hello world", the result should be ["hello", "world"].

### Objects

Create an Object: Write a function createPerson that creates and returns an object representing a person. The object should have properties like name, age, and gender.
// Example: createPerson("Alice", 30, "female");

Create an Object: Write a function Person that creates and returns an object representing a person. The object should have properties like name, age, and gender. Function Person must be called with the keyword new.
// Example: new Person("Alice", 30, "female");

Access Object Properties: Create a function getProperty that takes an object and a property name as arguments and returns the value of that property.
// Example: getProperty({ name: "Alice", age: 30 }, "name"); // Returns "Alice"

Add a Property to an Object: Write a function addProperty that adds a new property city to an existing object.
// Example: const person = { name: "Alice", age: 30 };
// addProperty(person, "city", "New York"); // person is now { name: "Alice", age: 30, city: "New York" }

Delete a Property from an Object: Create a function deleteProperty that removes a specified property from an object.
// Example: const person = { name: "Alice", age: 30, city: "New York" };
// deleteProperty(person, "city"); // person is now { name: "Alice", age: 30 }

Check if a Property Exists: Write a function hasProperty that checks if a given property exists in an object.
// Example: hasProperty({ name: "Alice" }, "name"); // Returns true

Merge Two Objects: Create a function mergeObjects that merges two objects into one.
// Example: mergeObjects({ name: "Alice" }, { age: 30 }); // Returns { name: "Alice", age: 30 }

Clone an Object: Write a function cloneObject that creates a shallow copy of an object.
// Example: const original = { name: "Alice" };
// const copy = cloneObject(original); // copy is { name: "Alice" }

Get Object Keys: Create a function getObjectKeys that returns an array of all the keys in an object.
// Example: getObjectKeys({ name: "Alice", age: 30 }); // Returns ["name", "age"]

Get Object Values: Write a function getObjectValues that returns an array of all the values in an object.
// Example: getObjectValues({ name: "Alice", age: 30 }); // Returns ["Alice", 30]

Count Properties in an Object: Create a function countProperties that counts the number of properties in an object.
// Example: countProperties({ name: "Alice", age: 30 }); // Returns 2

Iterate Over Object Properties: Write a function logProperties that logs each key-value pair in an object.
// Example: logProperties({ name: "Alice", age: 30 }); // Prints name: "Alice" and on the next line age: 30

Create an Object from an Array: Create a function arrayToObject that takes an array of key-value pairs and converts it into an object.
// Example: arrayToObject([["name", "Alice"], ["age", 30]]); // Returns { name: "Alice", age: 30 }

Deep Clone an Object: Write a function deepClone that creates a deep copy of an object.
// Example: const original = { name: "Alice", address: { city: "New York" } };
// const copy = deepClone(original); // copy is a deep clone of original

Find the Maximum Value in an Object: Create a function findMaxValue that finds the maximum value among the properties of an object that are numbers.
// Example: findMaxValue({ a: 1, b: 5, c: "hello" }); // Returns 5

Filter Object Properties: Write a function filterProperties that filters the properties of an object based on a condition.
// Example: filterProperties({ a: 1, b: 2, c: 3 }, value => value > 1); // Returns { b: 2, c: 3 }

Transform Object Values: Create a function transformValues that transforms the values of an object using a provided callback function.
// Example: transformValues({ a: 1, b: 2 }, value => value \* 2); // Returns { a: 2, b: 4 }

Get Nested Object Property: Write a function getNestedProperty that retrieves a value from a nested object using a path string.
// Example: getNestedProperty({ user: { address: { city: "New York" } } }, "user.address.city"); // Returns "New York"

Flatten a Nested Object: Create a function flattenObject that flattens a nested object into a single-level object with concatenated keys.
// Example: flattenObject({ user: { name: "Alice", age: 30 }, city: "New York" });
// Returns { "user.name": "Alice", "user.age": 30, city: "New York" }

Sort Object Keys: Write a function sortObjectKeys that returns a new object with its properties sorted by key.
// Example: sortObjectKeys({ b: 2, a: 1 }); // Returns { a: 1, b: 2 }

Transform Object Keys: Create a function transformKeys that transforms the keys of an object using a provided callback function.
// Example: transformKeys({ a: 1, b: 2 }, key => key.toUpperCase()); // Returns { A: 1, B: 2 }

Count Occurrences of Values: Write a function countValueOccurrences that counts how many times each value appears in an object.
// Example: countValueOccurrences({ a: 1, b: 2, c: 1 }); // Returns { 1: 2, 2: 1 }

Pick Specific Properties: Create a function pickProperties that takes an object and an array of property names, returning a new object with only those properties.
// Example: pickProperties({ name: "Alice", age: 30, city: "New York" }, ["name", "city"]);
// Returns { name: "Alice", city: "New York" }

Omit Specific Properties: Write a function omitProperties that takes an object and an array of property names, returning a new object without those properties.
// Example: omitProperties({ name: "Alice", age: 30, city: "New York" }, ["age"]);
// Returns { name: "Alice", city: "New York" }

Get Property Types: Create a function getPropertyTypes that returns an object with the types of each property in the original object.
// Example: getPropertyTypes({ name: "Alice", age: 30 }); // Returns { name: "string", age: "number" }

Group Objects by Property: Write a function groupByProperty that takes an array of objects and groups them by a specified property.
// Example: groupByProperty([{ name: "Alice", city: "New York" }, { name: "Bob", city: "New York" }, { name: "Charlie", city: "Chicago" }], "city");
// Returns { "New York": [{ name: "Alice", city: "New York" }, { name: "Bob", city: "New York" }], "Chicago": [{ name: "Charlie", city: "Chicago" }] }

Find Common Properties: Write a function findCommonProperties that takes two objects and returns an array of properties that are common to both objects.
// Example: findCommonProperties({ a: 1, b: 2 }, { b: 3, c: 4 }); // Returns ["b"]

Get Unique Properties: Create a function getUniqueProperties that takes two objects and returns an array of properties that are unique to each object.
// Example: getUniqueProperties({ a: 1, b: 2 }, { b: 3, c: 4 }); // Returns ["a", "c"]

Create a Default Object: Write a function createDefaultObject that creates an object with default properties and allows overriding some of them.
// Example: createDefaultObject({ name: "Alice" }); // Returns { name: "Alice", age: 0, city: "Unknown" }

Transform Object to CSV: Write a function arrayToCSV that converts an array of objects into CSV format.
// Example: arrayToCSV([{ name: "Alice", age: 30 }, { name: "Bob", age: 25 }]);
// Returns "name,age\nAlice,30\nBob,25"

Create a Read-Only Object: Write a function createReadOnlyObject that takes an object and returns a read-only version of it.
// Example: const person = createReadOnlyObject({ name: "Alice" });
// person.name = "Bob"; // Throws an error in strict mode
